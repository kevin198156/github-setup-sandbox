# github-setup-sandbox

> **Lifecycle: SANDBOX / HISTORICAL SETUP TEST**

This public repository was used to test GitHub/development setup and instruction behavior. Its checked-in `CLAUDE.md` is sandbox material and is **not** current global ChatGPT/Codex policy.

## Dependency note

The private `personal-project-core` repository contains a Git object entry named `github-setup-sandbox` pointing at this repository's historical commit.

## Maintenance boundary

- Do not use this repository as the Source of Truth for unrelated projects.
- Do not delete or rename it until the reference from `personal-project-core` is reviewed.
- New development should occur in the actual target repository rather than extending this sandbox.
