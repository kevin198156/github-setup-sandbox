# 系統最高指導原則 (System Directives)
# 版本：1.1 | 環境：Windows x64 雙機 | 狀態：GitHub 過渡期

## 1. 核心邊界與安全紀律
- 絕對禁止使用簡體字或大陸用語，一律使用繁體中文（台灣習慣用語）輸出。
- 絕對禁止在對話中印出密碼、金鑰、Token 等機密資料，必須從環境變數或 `.env` 讀取。
- 絕對禁止執行任何可能清空系統根目錄或進行不可逆批量刪除的指令（如 `rm -rf /`、
  `rmdir /s /q C:\` 等），此為最高級別違規，與作業系統無關。
- 越權確認：執行任何指令前必須先確認當前工作目錄；存取上層目錄必須先獲得明確同意。
- 漸進式載入：保持本檔案精簡，具體的專案邏輯與技術架構請查閱專案內的 README.md。

## 2. 資訊獲取與查證
- 絕對避免幻覺：使用任何 API 或撰寫核心邏輯前，強制使用 MCP 或 Search 工具查閱最新官方文件。
- 時空校準：查證資訊必須以當下時間與地點（中華民國台灣台北市）及專案目標進行 5W1H 校對。
- 避免重複：行動前若 `MEMORY.md` 與 Git 歷史存在，則先讀取；若不存在，跳過此步驟繼續執行。
- 定期清理：發現專案內有過時的規則或設定檔，必須主動提議刪除，避免誤導決策。

## 3. 環境與 CLI 預設習慣（Windows x64 原生環境）
- CLI 優先：能用命令列完成的任務，絕對優先使用內建 CLI 工具。
- 容器化預設：系統服務預設使用 `docker`（需 WSL2 後端），執行前先確認 WSL2 是否就緒。
- 套件管理：Windows 原生環境優先使用 `winget` 或 `scoop`；
  `brew` 僅在 WSL2 Shell 內使用，不適用於 PowerShell / CMD。
- Python 規範：絕對禁止使用原生 `pip` 全域安裝，一律使用 `uv` 建立虛擬環境或 `uvx` 執行。
- Node 規範：使用 `nvm-windows` 管理版本（注意：與 Unix nvm 指令語法略有差異），
  套件用 `npm`，執行用 `npx`。如需跨平台一致性，可改用 `fnm`。

## 4. 開發與測試流程（含 GitHub 過渡期機制）
- 測試絕對優先：強制要求撰寫測試（單元、功能），嚴格要求覆蓋率與 User Story 對齊。
- 端到端防護：若是 Agent 開發，必須善用 Computer Use 與瀏覽器工具完成 E2E 測試。
- 版本控制（依 DEPLOYMENT.md 狀態執行）：
  - 執行版控步驟前，先讀取專案根目錄的 `DEPLOYMENT.md` 確認當前備份狀態。
  - 狀態為 `local-only`（過渡期）：
    1. 確認專案資料夾已執行 `git init`，在本機維護完整 Git 歷史。
    2. 達成重要里程碑時，在專案同層的 `_backups/` 目錄產出壓縮快照，
       命名格式：`專案名_YYYYMMDD_HHMM.zip`。
    3. 不嘗試推送遠端，不因缺少 GitHub 授權而阻塞主流程。
  - 狀態為 `github-ready`：推送至對應的 GitHub public repo（以專案資料夾為名），
    並維護標準 `README.md`。

## 5. 跨裝置環境感知 (Cross-Device Awareness)
- 本專案會頻繁在「工作筆電」與「高階桌機」雙環境（皆為 Windows x64）間切換。
- 絕對禁止在程式碼中寫死與硬體綁定的極限值（如記憶體上限、執行緒數量）。
- 執行耗能任務前，必須使用 `systeminfo` 或 Python 的 `psutil` 動態偵測 CPU 與 RAM，
  並依以下基準自動調整策略：
  - 可用 RAM ≥ 16GB：標準模式，不限制並行數。
  - 可用 RAM 8–16GB：保守模式，並行任務數上限減半。
  - 可用 RAM < 8GB：低耗模式，強制序列執行並通知使用者。
